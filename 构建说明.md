# 哒哒宝贝 Android APK 构建说明

## 📦 项目说明

这是一个完整的Android项目，包含"哒哒宝贝"幼儿互动乐园的所有功能。

## 🚀 快速构建APK

### 方法一：使用 Android Studio（推荐）

1. **下载并安装 Android Studio**
   - 访问 https://developer.android.com/studio
   - 下载并安装最新版

2. **打开项目**
   - 打开 Android Studio
   - 选择 "Open an existing Android Studio project"
   - 选择本项目文件夹

3. **构建APK**
   - 等待 Gradle 同步完成
   - 点击菜单栏 `Build` → `Build Bundle(s) / APK(s)` → `Build APK(s)`
   - APK 将生成在 `app/build/outputs/apk/debug/app-debug.apk`

### 方法二：使用命令行

1. **安装 JDK 17+**
   ```bash
   # Ubuntu/Debian
   sudo apt install openjdk-17-jdk
   
   # macOS
   brew install openjdk@17
   ```

2. **安装 Android SDK**
   - 下载命令行工具：https://developer.android.com/studio#command-tools
   - 解压并设置环境变量

3. **构建APK**
   ```bash
   cd dada-android-project
   ./gradlew assembleDebug
   ```

## 📱 安装APK

构建完成后，APK文件位于：
```
app/build/outputs/apk/debug/app-debug.apk
```

安装到设备：
```bash
adb install app/build/outputs/apk/debug/app-debug.apk
```

## ⚙️ 项目配置

### 应用信息
- **包名**: `com.dada.app`
- **应用名称**: 哒哒宝贝
- **最低SDK**: 21 (Android 5.0)
- **目标SDK**: 34 (Android 14)

### 权限说明
- `INTERNET` - 网络访问（AI对话、图片生成）
- `RECORD_AUDIO` - 录音（语音输入）
- `CAMERA` - 相机（拍照功能）
- `READ_EXTERNAL_STORAGE` - 读取存储（图片浏览）
- `WRITE_EXTERNAL_STORAGE` - 写入存储（保存作品）

### 屏幕适配
- 默认横屏模式
- 16:9 屏幕比例优化
- 支持平板设备

## 📂 项目结构

```
dada-android-project/
├── app/
│   ├── src/
│   │   └── main/
│   │       ├── AndroidManifest.xml    # 应用配置
│   │       ├── assets/                # Web资源
│   │       │   ├── index.html
│   │       │   ├── images/
│   │       │   └── ...
│   │       ├── java/com/dada/app/
│   │       │   ├── MainActivity.java   # 主Activity
│   │       │   └── MyWebViewClient.java # WebView客户端
│   │       └── res/                   # 资源文件
│   │           ├── layout/            # 布局文件
│   │           ├── mipmap-*/          # 应用图标
│   │           └── values/            # 字符串、样式
│   └── build.gradle                   # 模块构建配置
├── build.gradle                       # 项目构建配置
├── gradle.properties                  # Gradle配置
├── gradlew                            # Gradle包装器
└── settings.gradle                    # 项目设置
```

## 🎨 自定义配置

### 修改应用图标
替换以下目录中的图标文件：
- `app/src/main/res/mipmap-mdpi/ic_launcher.png`
- `app/src/main/res/mipmap-hdpi/ic_launcher.png`
- `app/src/main/res/mipmap-xhdpi/ic_launcher.png`
- `app/src/main/res/mipmap-xxhdpi/ic_launcher.png`
- `app/src/main/res/mipmap-xxxhdpi/ic_launcher.png`

### 修改应用名称
编辑 `app/src/main/res/values/strings.xml`：
```xml
<string name="app_name">你的应用名称</string>
```

### 修改包名
1. 编辑 `app/build.gradle`：
   ```gradle
   applicationId "com.yourcompany.yourapp"
   ```
2. 重命名 `java/com/dada/app/` 目录
3. 修改 Java 文件中的包名

## 🔧 常见问题

### Gradle 同步失败
```bash
# 清除Gradle缓存
./gradlew clean
# 重新同步
./gradlew build
```

### 构建失败
```bash
# 检查SDK路径
export ANDROID_HOME=/path/to/android-sdk
# 重新构建
./gradlew assembleDebug --stacktrace
```

### 安装失败
```bash
# 启用USB调试
adb devices
# 重新安装
adb install -r app-debug.apk
```

## 📞 技术支持

如有问题，请参考：
- Android Studio 文档：https://developer.android.com/studio/intro
- Gradle 文档：https://docs.gradle.org/

---

**祝你构建成功！** 🎉
